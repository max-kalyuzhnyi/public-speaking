# Lecture visuals — `Сбер_оценка рынка_Апр2025.pdf`

- **`slide-NN.png`** — raster export of PDF page `NN` (1-based), generated with `pdftoppm` at 110 DPI.
- **`pdf_audit.tsv`** — per-page `pdftotext` character count vs embedded image count; use `suggestion` to choose **OCR** vs **text_ok** (see `tools/pdf_page_audit.sh`).
Replace individual PNGs after editing in Keynote/Pencil; keep filenames stable so Markdown links stay valid.
