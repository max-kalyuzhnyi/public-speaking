# Client logos (optional)

Place vector or raster logos here and keep filenames aligned with `brand.json`:

- `sber-logo.svg` (or `.png`) — referenced by `clients.sber.logo`
- `vtb-logo.svg`
- `skolkovo-logo.svg`

Do not commit third-party marks if your license does not allow it; swap paths in `brand.json` to local-only filenames.
